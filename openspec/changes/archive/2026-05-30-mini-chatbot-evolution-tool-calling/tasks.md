## 1. 工具协议与执行器

- [x] 1.1 新增 `internal/tools/tool.go`，定义工具元信息、参数描述、`ToolSpec`、`ToolCall`、`ToolResult` 和结构化错误类型
- [x] 1.2 新增 `internal/tools/executor.go`，实现工具注册、重复注册拒绝、未知工具错误、参数校验入口和执行分发
- [x] 1.3 为执行器补充单元测试，覆盖注册成功、重复注册、未知工具、参数错误、执行成功和执行失败

## 2. 首版四个工具

- [x] 2.1 实现 `calculator` 工具，仅支持安全基础数学表达式，覆盖合法表达式、非法表达式、空输入和除零测试
- [x] 2.2 实现 `time` 工具，返回当前日期、时间、时区和 Unix 时间戳，并补充格式测试
- [x] 2.3 实现 `file_read` 工具，限制工作区内文本读取，拒绝 `.git/`、`.claude/`、`.codex/`、敏感路径和超大文件，并补充边界测试
- [x] 2.4 实现 `web_search` 工具和 `SearchClient` 接口，支持查询、结果数量上限、搜索失败和未配置错误，并使用 fake client 覆盖测试

## 3. ChatBot 工具调用闭环

- [x] 3.1 在 ChatBot 主流程中接入工具执行器，支持模型返回结构化 `tool_call` 时执行对应工具
- [x] 3.2 将 `tool_result` 作为受控上下文回填给模型，并执行第二次模型调用生成最终文本回答
- [x] 3.3 限制单轮最多一次工具调用；第二次模型响应再次请求工具时不继续执行，并基于已有工具结果生成兜底最终回答
- [x] 3.4 保证工具执行失败或模型二次调用失败时不会追加错误的 assistant 历史

## 4. 配置、文档与验证

- [x] 4.1 为 `web_search` 增加最小可选配置或不可用实现，未配置时返回结构化错误而不是启动失败
- [x] 4.2 更新 `configs/config.example.yaml`、`README.md` 和 `docs/evolution.md`，说明工具范围、安全边界和暂不支持能力
- [x] 4.3 补充 ChatBot 集成测试，覆盖 `calculator`、`time`、`file_read`、`web_search` 成功路径和失败回退
- [x] 4.4 运行 `go test ./...`，确认新增工具与既有聊天、Prompt、上下文控制测试全部通过
